// @ts-ignore not used variable yet
export const renderList: <T>(renderFn: (obj: T) => string) => (items: T[]) => string = <T>(
    // @ts-ignore not used variable yet
    renderFn: (obj: T) => string
) => {
    // TODO: Start here
    // @ts-ignore not used variable yet
    return (items: T[]) => "";
};
